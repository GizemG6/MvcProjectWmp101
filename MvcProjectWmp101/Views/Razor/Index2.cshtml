
@{
    ViewBag.Title = "Index2";
}

<h2>Index2</h2>
<h3>Razor ile karar yapilari</h3>

@{
    string[] names = new string[3] { "Gizem", "Gunes", "GG" };
    int index = 0;
    foreach (string name in names)
    {
        @name
        <br />
    }
    <ul>
        @foreach (var name in names)
        {
            <li>@name</li>
        }

    </ul>
}
<br />
<hr />
<ul>
    @foreach (string name in names)
    {
        <li>
            @if (index % 2 == 0)
            {
                <b>@name</b>
            }
            else
            {
                <i style="color:red; font-size:20px;">@name</i>
            }
        </li>
        index++;
    }

</ul>
<br />
<hr />
@for (int i = 0; i < 10; i++)
{

    @("Number is "+ i)
    <br />
}
@for (int i = 0; i < 10; i++)
{
    <li>
        @if (i % 2 == 0)
        {
            <b style="color:blue; font-size:20px;">@("Number is "+ i)</b>
        }
        else
        {
            <b style="color:red; font-size:20px;">@("Number is "+ i)</b>
        }
    </li>
}
